// Drawer overlays animate in immediately — avoid flashing the old feed skeleton
// over the explore screen while the intercepted route streams in.
export default function DrawerLoading() {
  return null;
}
